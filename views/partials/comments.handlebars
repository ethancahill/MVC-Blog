<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="text">
      {{comment_text}}
    </div>
     <div class="meta">
     by {{user.username}} on {{format_date created_at}}
    </div>
  </section>
  <br>
  {{/each}}
</div>